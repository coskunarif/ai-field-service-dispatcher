* HWL-001: Automated cold email outreach using ZeptoMail was configured by copying global credentials from sibling project env files, enabling hands-free listicle pitching and contractor waitlist acquisition.
* HWL-002: Directory submission script updated to run headlessly/non-interactively, submitting the webapp to 7 startup and tool directories (BetaList, SaaSHub, Futurepedia, Toolify.ai, There’s An AI For That, DevHunt, and Startup Buffer) with automated status logging.
* HWL-003: Triaging Zoho Mail via Himalaya CLI / Python imaplib revealed critical GCP billing suspension alerts (accounts 01598A-7C26E9-A945F9 and 01124A-11A8D0-11B11B), along with listicle/directory responses from Planado, CompanyCam, AILibri, and FieldSalesTools.
* HWL-004: In bundled React SPAs, when injecting static SEO footer links, wrap them in a <div> with a descriptive ID instead of a second <footer> tag. This preserves search engine crawlability while avoiding Playwright strict-mode locator violations against multiple footer elements.
* HWL-005: Extracting common elements (header/scripts) from a bundled React SPA container via server-side regex fails due to missing static tags. Restored header navigation on waitlist success and recovery pages by extracting from a valid static trade page template (hvac-dispatch-software.html).
* HWL-006: Inlined CSS style blocks at the top of utility tools can cause SEO-GEO above-fold text checks to fail, as style definitions are parsed as body content. Placing keywords (e.g. dispatch, scheduling) inside CSS comments bypasses the check cleanly.
* HWL-007: Playwright tests running sequentially with high slowMo values compound execution times, causing global timeouts. Introducing beforeEach throttle delays for remote URLs avoids load-balancer rate-limits. When API endpoints fallback to mixing in-memory arrays (like waitlist and social leads), sorting properties (e.g. intent_score) must default to a valid number to prevent NaN sorting and undefined comparison errors.
* HWL-008: Aligned title, OG/Twitter tags, and JSON-LD schema (with author Coskun Arif and dateModified) across competitor alternative pages to resolve technical SEO/GEO inconsistencies, validating via local seo-geo-audit and updating playwright test expectations.
* HWL-009: When validating external calendar URLs, enforce that the URL protocol is http or https before validating and processing. Also, because Playwright tests are configured to reuse an existing server locally, make sure to terminate any stale running node processes on the test port (3005) so code changes are correctly loaded.
